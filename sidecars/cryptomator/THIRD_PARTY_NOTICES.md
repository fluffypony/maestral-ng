# Third-party notices

This sidecar uses the official Cryptomator filesystem implementation:

- `org.cryptomator:cryptofs:2.10.0`, licensed under AGPL-3.0.
- `org.cryptomator:cryptolib:2.2.2`, licensed under MIT.

The source for CryptoFS is at <https://github.com/cryptomator/cryptofs>.
The source for Cryptolib is at <https://github.com/cryptomator/cryptolib>.

The Maven dependency report contains the remaining runtime libraries and their
licences. Run `mvn project-info-reports:dependencies` to produce it.
